# Project planning:

### Questions to ask yourself before writing any code:

1. What are the main containers of elements I need in this app?
  - a contaners that consists as a life in the name of programming language
  - a container where users will put its input
  - a container where user will type the input

2. What values will need to be saved in state vs. what values can be derived from the state?
  - language container will be put in to the container. 
  - Input container where the state will be dervied from 
  - keyboard container it is changing it color
  
3. How will the user interact with the app? What events do I need to handle?
  - Keyboard Input and the the event that lets you interact with textinput in real time. 